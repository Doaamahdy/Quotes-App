import 'package:flutter/material.dart';

class AppTextLarge extends StatelessWidget {
  const AppTextLarge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
